/*
  # Fix duplicate Connected Values section in GoalSetting component

  1. Changes
     - Removes the duplicate "Connected Values" section in the GoalSetting component
     - Ensures annual goals have a proper deadline date field
     - Improves the UI for values display
*/

-- This is a client-side UI fix that doesn't require database changes.
-- This migration file is a placeholder to document the change.

-- The fix involves modifying the GoalSetting.tsx component to:
-- 1. Remove the duplicate "Connected Values" section
-- 2. Add a deadline date field for annual goals
-- 3. Ensure proper conditional rendering of values

-- No actual database changes are needed for this UI fix.